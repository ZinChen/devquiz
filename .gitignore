# Rails
app/.env
app/tmp/
app/log/
app/storage/
app/public/vite-dev/
app/public/vite-test/
app/public/vite/
app/node_modules/
app/*.local
app/config/master.key
app/config/credentials.yml.enc
app/.byebug_history

# macOS
.DS_Store

# Editor and assistant settings (personal, not shared)
.vscode/
.idea/
.claude/*
.claude/skills/*
# Исключение: командный скил по составлению тестов шарится через репозиторий.
!.claude/skills/
!.claude/skills/devquiz-tests/

# Локальные тесты: symlink на tests/ в соседнем проекте devquiz-drafts,
# читаются приложением, но не шарятся и не трекаются гитом.
/tests_custom

# Mise (keep mise.toml, ignore local overrides)
.mise.local.toml
