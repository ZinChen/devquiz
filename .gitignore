# Rails
app/.env
app/tmp/
app/log/
app/storage/
app/public/vite-dev/
app/public/vite-test/
app/public/vite/
app/node_modules/
app/*.local
app/config/master.key
app/config/credentials.yml.enc
app/.byebug_history

# macOS
.DS_Store

# Editor and assistant settings (personal, not shared)
.vscode/
.idea/
.claude/

# Локальные тесты: лежат рядом с tests/, читаются приложением, но не шарятся.
# README нужен, чтобы папка не выглядела случайной у того, кто её найдёт.
/tests_custom/*
!/tests_custom/README.md

# Mise (keep mise.toml, ignore local overrides)
.mise.local.toml
