<!DOCTYPE html>
<html lang="ru">
  <head>
    <title><%= content_for(:title) || "DevQuiz" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <%= vite_client_tag %>
    <%= vite_javascript_tag 'application', defer: true %>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
